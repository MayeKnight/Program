1.环境安装
Note: ultralytics(yolov8, yolov8现在改名了..)
本操作基于先前已整理的"pytorch"目录下的操作, 亦即我们已安装了anaconda.
(1)创建虚拟环境, 避免默认环境越来越臃肿
在 anaconda prompt 下
conda create -n yolov8 python=3.8   其中 -n 后跟创建环境的名字, python=3.8指明创建的版本

随后可以通过
conda activate yolov8  激活名为yolov8的环境
"这个点也是接下来需要注意的, 我们后续的所有操作都需要在当前的环境下进行, 亦即在执行yolov8相关操作时, 要激活名为yolov8的环境这步是十分重要的."

(2)配置国内源, 避免因为要从国外地址下载而影响下载速率
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

(3)安装pytorch环境
由于每个conda环境都是相互隔离的, 安装的包只存在于当前激活的环境中, 虽然先前我们有一个名为pytorch的环境安装了PyTorch, 但新建的名为yolov8的环境是一个全新的环境, 
目前只包含了 Python 3.8 和最基本的依赖包, 里面并没有 PyTorch. 如果要在名为yolov8的环境中运行 YOLOv8 (它依赖PyTorch) , 就必须在这个环境中安装 PyTorch.
conda install pytorch==2.1.1 torchvision==0.16.1 torchaudio==2.1.1 pytorch-cuda=12.1 -c pytorch -c nvidia

(4)安装ultralytics(YOLOv8)
官方文档: https://docs.ultralytics.com/zh/

在下载好的ultralytics源码目录(D:\Tech\Code\Program\yolov8\ultralytics-main)下(且在之前创建好的名为yolov8的环境下)
pip install -e .   //通过源码安装
如遇SSL链接问题报错
pip install -e . -i https://pypi.org/simple  //绕过有问题的清华源进行安装即可

采用这种安装方式, 我们后续在代码上修改, 它就能检测到了

(5)如何正确使用windows中的终端
除了在 anaconda prompt 中可以使用yolov8
在 cmd (命令提示符)中其实也可以使用, 不过要先进行初始化操作
在 cmd 中输入
conda init cmd.exe  //先让cmd进行下初始化, 随后关闭cmd, 重新打开
conda activate yolov8  //就可在cmd中激活创建的名为yolov8的环境
(在powershell中不能激活环境)
因此在vscode中的终端, 我们也要选择为cmd, 而不是powershell, 这样我们后续启动的所有终端都是cmd的终端, 这样就可以避免后续的很多问题.


2.模型预测
(1)模型预测的基本使用
yolov8支持两种调用方式, 一种是基于命令行的方式, 如下述所示
yolo task=detect mode=predict model=./yolov8n.pt source="./ultralytics/assets/bus.jpg"
//参数含义如下
//yolo 启动命令
//task=detect 指定做什么任务, detect就是目标检测任务
//mode=predict 这个参数mode就是让它进行'预测'还是'训练'还是'验证'
//model=./yolov8n.pt  我们要使用哪个模型进行预测
//source="./ultralytics/assets/bus.jpg"  这里的source就是对谁进行检测
//经过这个命令, 它就可以对这个图片进行推理了

以下操作在"D:\Tech\Code\Program\yolov8\ultralytics-main"目录下的终端中进行, 可以通过 cd 文件目录 到目标目录
conda activate yolov8  //激活先前创建的yolov8环境
yolo detect predict model=./yolov8n.pt source="./ultralytics/assets/bus.jpg"   //'task='和'mode='可省略, 
//执行结果以图片形式存放到"D:\Tech\Code\Program\runs\detect"目录下, 在终端中会输出结果, 如本例输出"4 persons, 1 bus, 1 stop sign"

我们先看一下它所对应的代码形式是怎么样的, 在yolov8里面, 我们在做检测的时候就并不需要我们再有一个完整的detect.py了, 我们只需要有三行代码就能检测了
"D:\Tech\Code\Program\yolov8\ultralytics-main\program\demo_predict.py"路径下演示一下

接下来讲一些常用的参数, 因为如果只是以上述方式运行的话, 会发现其实什么也得不到, 我们就引出一些常用的参数, 来控制它, 得到一个我们想要的结果
//可选参数
官方提供的在"D:\Tech\Code\Program\yolov8\ultralytics-main\ultralytics\cfg\default.yaml"目录下能找到, 在这里面包含了所有可能用到的参数
在这里面能找到"Predict settings"就是模型预测部分要使用的参数了
当然这里的参数也不是一个非常全的版本, 要想看到全的版本, 要去官网上的官方文档中右非常详细的参数的介绍(一般来说不会用到那么多)
比方说常用的
source: # source directory for images or videos, 用于指定模型推理时的输入源, 可以是单个图片或视频文件的路径(如"bus.jpg"), 图片或文件夹的路径(会处理文件夹内所有支持的格式), 视频流地址(如Bilibili连接), 整数(如0, 表示使用第一个摄像头的设备), 或者屏幕(参数为source="screen"), 或一个图像/视频的列表
show: False  # show results if possible, 要不要检测完成后, 把检测结果展现出来
save_txt: False  # save results as .txt file, 是否在保存目标检测结果时, 将每个检测到的目标以.txt的形式保存下来
save_conf: False # save results with confidence scores, 是否在保存目标检测结果时, 将每个检测到的目标所对应的置信度分数(confidence score)也一同保存下来, 置信度是模型对预测结果的确信程度, 通常是一个0到1之间的数值, 反映模型认为"当前检测框内确实包含目标, 并且框的位置和大小准确"的把握
save_crop: False # save cropped images with results, 是否保存裁剪后的目标图像(即只包含检测到的目标区域的子图像, 而不是保存整张标注图像)
hide_labels: False # hide labels, 是否要隐藏labels
hide_conf: False # hide confidence scores, 是否隐藏置信度
vid_stride: 1 # video frame rate stride, 用于视频检测时控制采样的帧间隔, 即每隔多少帧处理一帧, 默认值1(逐帧处理, 即对视频的每一帧都进行检测), 值大于1, 例如 vid_stride=2, 则只处理1、3、5帧(每隔1帧), 跳过中间的偶数帧, 这样可以大幅提升处理速度, 尤其在视频帧率很高(如30fps)且检测任务不需要每帧都精准时, 可以平衡速度和准确性

举个例子, 如对于参数source
如对于"D:\Tech\Code\Program\yolov8\ultralytics-main\program\demo_predict.py"
result = yolo(source=r"D:\Tech\Code\Program\yolov8\ultralytics-main\ultralytics\assets\bus.jpg")  #对这个图片进行检测, 在python环境下, 要改为\表示转义, 所以要改为\\, 或者在字符串前的r用于取消转义
还有就是可以对屏幕进行检测, 需要设置参数为 source="screen"
result = yolo(source="screen")  #对当前屏幕进行检测, 刚开始运行的时候会发现没检测到什么东西, 但我们可以把图片切出来, 就能检测到了人了, 但由于我们打开的图片并不是规定的大小, 所以会把它压缩, 所以最终它所检测到的结果跟我们直接输入的图片是有出入的, 随着我们调动桌面(ctrl+滚轮, 改变图片大小), 检测结果就会有所变化
此外, 如果我们想打开摄像头的话, 需要设置参数为 source=0
result = yolo(source="screen")  #对摄像头内容进行检测, 这里由于我们正在坐在摄像头前面, 所以它会检测出"0: 480x640 1 person, 4.7ms", 1个person

如果说我们需要往下保存模型的话, 就要把 save 参数指定上, save=True, 这样它就会将结果存在 runs 目录下面, 其中的predict文件夹下bus.jpg图片所示即为最终检测结果
result = yolo(source=r"D:\Tech\Code\Program\yolov8\ultralytics-main\ultralytics\assets\bus.jpg", save=True)  #其它参数与此类似, 使用的话, 在这里指定上即可

额外重点的需要再说两个参数conf, IoU
-(1)conf(置信度, Confidence Score): 模型对某个检测框的"确信程度", 通常是一个0到1之间的数值, 如runs 目录下面, 其中的predict文件夹下bus.jpg图片中的数值其实就是conf, 置信度
--它综合了两个方面:
框内包含目标的概率(objectness)
预测框与真实框的匹配精度(通常由IoU决定)
--作用:用于筛选预测结果
在推理时, 可以通过设置置信度阈值(如conf=0.25)来过滤掉低置信度的预测, 只保留模型比较确信的结果
eg.
result = model.predict(source="bus.jpg", conf=0.5)  # 只保留置信度≥0.5的框
结合先前内容即
result = yolo(r"D:\Tech\Code\Program\yolov8\ultralytics-main\ultralytics\assets\bus.jpg", save=True, conf=0.5)  #只保留置信度>=0.5的框, 可见runs目录下先前所得到的predict4中bus.jpg的置信度为0.26的框, 并没有在本行代码运行后的predict5的bus.jpg中出现  (即conf值设置的越高, 可能出现的框越少)
-(2)IoU(交并比, Intersection over Union): 预测框与真实框(或另一个预测框)的重叠程度, 计算公式为
--含义
IoU = Area of Overlap / Area of Union, 值也在0和1之间, 越大表示框重合度越高
其中
Area of Overlap(交集面积): 指两个框(例如预测框和真实框)重叠区域的面积
Area of Union(并集面积): 指两个框覆盖的总面积
计算公式为 
Union = Area(A) + Area(B) - Overlap
--作用
评估定位精度:判断预测框是否"准"
非极大值抑制(NMS, Non-Maximum Suppression):去除同一目标上的多个重叠框, 是目标检测中常用的后处理步骤, 它的核心思想是, 保留局部置信度最高的框作为"基准框", 计算该基准框与剩余每个框的IoU(交并比), 若某个剩余框与基准框的IoU大于预设阈值(如0.5), 则认为它们检测的是同一个物体, 则该剩余框移除(抑制)  (iou值设置越高, 可能出现的框就越多)
结合先前内容即
result = yolo(r"D:\Tech\Code\Program\yolov8\ultralytics-main\ultralytics\assets\bus.jpg", save=True, iou=0.7)  #NMS 时 IoU 阈值设为 0.7

简单理解
"conf"回答"这个框可信吗?", 用于初步筛选, 决定哪些框值得保留(基于置信度)
"IoU"回答"这个框和另一个框有多像?", 在初步筛选后, 通过 NMS 去掉重叠度高的冗余框, 保证每个目标只有一个最终结果


(2)检测结果使用拓展(Boxes信息, 结果可视化(要将BGR转为RGB))
-Note:
--1.Boxes信息:在目标检测模型(如YOLOv8)的输出中, Boxes是指模型预测的所有边界框(bounding boxes)及其相关属性. 通过 result[0].boxes 可以访问这些信息, 通常包含以下内容
xyxy: 边界框的坐标, 格式为 [x1, y1, x2, y2], 表示左上角和右下角的像素坐标
conf: 置信度分数(confidence score), 范围0到1, 表示模型对该框的把握程度.
cls: 类别ID(整数), 对应模型训练时的类别索引.
xywh(可选): 边界框的中心坐标和宽高 [x_center, y_center, width, height], 常用于后续处理

--2.结果可视化与 BGR <-> RGB 转换
YOLOv8 (Ultralytics YOLO) 默认使用 OpenCV 读取图像, 
在 OpenCV 中, 默认的颜色通道顺序是 BGR (蓝-绿-红), 因此读取到的图像是BGR格式. 但在内部处理时, 模型本身需要 RGB 输入(因为模型训练时使用的是RGB数据), 所以 Ultralytics库 会在将图像送入模型前自动将 BGR 转换为 RGB. 
而大多数图像显示库(如 matplotlib, PIL) 以及网页图像使用的是 RGB (红-绿-蓝). 如果直接用 OpenCV 读取的图像(BGR) 交给 matplotlib 显示，颜色会异常(比如红色变成蓝色). 因此，在可视化时需要将 BGR 转换为 RGB。

-一般我们拿到检测结果后, 一般不仅仅是将它存到本地, 还要做一些额外的处理, 额外处理这部分的信息其实就是存在 Boxes 里面
"接下来的一系列操作是在jupyter中进行, 在jupyter中运行这部分的代码"
--对于vscode中使用jupyter, 如果是完全按照先前的方式安装的 yolov8, 是暂时用不了的, 需要在相应环境下的终端安装下
conda activate yolov8  //激活先前创建的yolov8环境
cd D:\Tech\Code\Program\yolov8\ultralytics-main  //打开工作目录
conda install jupyterlab  //安装jupyter
安装jupyter扩展
新建.ipynb文件(如"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\predict.ipynb")
打开该文件, 在右上角选择内核, 选择先前创建的yolov8环境, 随后就可以输入和运行(shift+enter)代码
如"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\predict.ipynb"所示
运行完第一个代码块后, 得到一个result, 可以在第二个代码块中输入result, 在 shift+enter 运行下看看 result 是什么, 发现result包含了一系列信息, 外层是一个list, 这里改为 result[0] 再运行下就得到了完整的 result 了
里面包含了 boxes, orig_img, orig_shape, path 等等信息

先来做绘图(检测结果可视化)这部分工作
yolov8提供了plt方法, 如上述jupyter文件中第三个代码块所示
import matplotlib.pyplot as plt  #使用Matplotlib 的 pyplot 模块 进行绘图和显示，所以也可以叫 “pyplot 方式”(通常称为"plt方式")  (也可写为import matplotlib.pyplot as mss, 语法上完全合法, as后面可以是任何有效的python标识符(字母, 数字, 下划线, 不能以数字开头), 但强烈建议使用plt, 因为 plt 是 Matplotlib 社区的标准别名，几乎所有文档、示例、教程都采用这个约定。使用非标准别名会让代码难以被他人理解，也容易与自定义变量混淆。除非有特殊理由（如避免命名冲突），否则应遵循惯例，使用 import matplotlib.pyplot as plt)
---
#检测结果可视化
# %matplotlib inline 是jupyter的魔法命令(magic command), 用于将 Matplotlib 绘制的图形直接嵌入到 Notebook 的单元格输出中, 而不是弹出独立的窗口.
# 魔法命令 后不能直接跟注释, 否则将导致解释器将 # 及其后的内容当成命令的参数, 导致报错
import matplotlib.pyplot as plt  #是python中导入模块并赋予别名的常用语法, matplotlin.pyplot 是matpoltlib库的一个子模块, 包含了一系列用于创建图形和绘图的函数(如 plot(), imshow(), show()等)， as plt 给导入的模块起一个别名, 之后在代码中可以用 plt 代替 matplotlib.pyplot, 使代码更简洁易读.
%matplotlib inline 
plt.imshow(result[0].plot()) 
# result是调用model.predict()返回的列表, 列表中的每个元素对应一张输入图像的检测结果(Result对象). 若只预测了一张图像, 则 result[0] 就是该图像的结果对象
# YOLO 的 Results 对象内置了 plot() 方法, 该方法会在原始图像上绘制检测框, 类别标签和置信度, 并返回一个 BGR 格式的 NumPy 数组 (蓝、绿、红, 因为底层使用 OpenCV 绘图), 这个数组可以直接用于显示、保存或进一步处理
# plt.imshow() 是 Matplotlib 的 imshow 函数用于显示图像, 默认情况下 imshow 期望输入的是 RGB格式 的图像数据 (即通道顺序为 红、绿、蓝)
---

如果如上述所示, 只使用 plot() 方法, 会发现得到的图颜色不正常, 这就是因为 plot() 得到的是 BGR 通道, 但直接输入到了 预期为 RGB 的 imshow() 中所致
如果要将 BGR 转为 RGB, 需要在后面再加一个转换, 如下述所示

---
import matplotlib.pyplot as plt  
%matplotlib inline 
plt.imshow(result[0].plot()[:,:,::-1]) 
---
Note: [:,:,::-1] 是一种通过 NumPy切片 将 BGR 图像转换为 RGB 图像, 这个技巧在 numpy 中有效, 因为图像数组 shape(H, W, 3), 其中第三个维度索引 0, 1, 2 分别对应 B, G, R, 翻转顺序后变为 R, G, B.
在三维数组 arr[:,:,::-1]中, arr是形状为(高度, 宽度, 通道)的 Numpy 数组(即(H, W, 3), 其中3为3通道, 这里指B, G, R), 通道顺序为 BGR

切片(slicing)基本形式是Python中用于从序列(如字符串, 列表, 元组, NumPy数组等)中提取子序列的一种灵活操作. 它通过指定[start:stop:step]三个参数来定义要选取的元素范围
基本语法
seq[start:stop:step]
start:起始索引(包含), 默认为0
stop:结束索引(不包含), 默认为序列长度.  (左闭右开区间)
step:步长, 默认为1. 可以为负数, 表示反向提取. 也可省略
eg.
s = [0, 1, 2, 3, 4, 5]
print(s[1:4])     # [1, 2, 3]   索引 1 到 3（不包含4）
print(s[:3])      # [0, 1, 2]   省略 start → 从开头
print(s[3:])      # [3, 4, 5]   省略 stop  → 直到末尾
print(s[::2])     # [0, 2, 4]   步长为 2
print(s[::-1])    # [5, 4, 3, 2, 1, 0]  步长 -1 → 反转
#负索引
print(s[-3:-1])   # [3, 4]  （从倒数第3到倒数第1，不包含倒数第1）
print(s[-1::-1])  # [5,4,3,2,1,0] 从末尾开始反向到开头
#重要特点
切片不会修改原序列, 而是返回一个新的序列(浅拷贝或视图, 取决于元素类型)(浅拷贝意味着创建了一个新的容器对象,但其中的元素（如果是可变对象）仍然引用原对象中的相同元素.  视图不复制数据，而是共享原始数组的内存块，只是以不同的形状或步长来访问数据。修改视图中的元素会直接影响原数组)
对于 NumPy数组, 切片返回的是原数组的视图(view), 修改视图会影响原数组(除非显式复制)
可以省略任何部分, 但必须保留冒号

[:,:,::-1]表示:
前两个维度(高度, 宽度)保持不变(:表示全部选取)
第三个维度(通道)使用 ::-1 即翻转通道顺序, 因此原本 [B, G, R] 变为 [R, G, B], 完成了 BGR -> RGB 的转换

依次执行"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\predict.ipynb"其中的代码块后发现得到的图颜色变为正常


--可视化说完了, 接下来看下如何取里面具体的信息
具体的信息其实都在 result 的 boxes 里面, 由于得到的 result 是一个序列, 而我们这里只导入了一张图片, 故这里为
result[0].boxes   # 即索引为0的第一张图片的 boxes 信息
可见 boxes 中包含很多信息, 比方说 boxes 的 data 一栏,  [2.2875e+01, 2.3128e+02, 8.0498e+02, 7.5684e+02, 8.7327e-01, 5.0000e+00], 前四个为属性坐标, 后面一个参数为置信度(confidence score), 最后一个是它的类别
cls 一栏则是非常明确的类别(class), 和上面的 data 的最后一个参数所在列是对应着的
conf 一栏则是和上面 data 中的 置信度 (confidence score) 所在列也是对应着的
xywh 给出中心点 x, y 的坐标, 再给出 宽w, 高h 一个总体的大小
xywhn 其中 n 即 normalize 归一化, 即在整个图片大小的基础上进行 normalize 归一化, 给出一系列分数
xyxy 即 xmin, ymin, xmax, ymax, 即左上角和右下角的坐标
通过这样的方式, 就可以把这个目标从图中框出来

---具体的引用, 可以通过 result[0].boxes.其他 来实现, 比方说
我们就想要 xywh, 就可以通过如下取到
result[0].boxes.xywh   #只不过这里得到的还是个 tensor 数据类型, 如果我们想把它转为 numpy 数据类型, 我们还需要先调用一下 cpu(), 再调用一下 numpy()
result[0].boxes.xywh.cpu().numpy()  # 调用下cpu(), 再调用下numpy(), 将 tensor数据类型 转为 numpy 数据类型
后续我们就可以根据 numpy 数据, 按照想要的方式将它从图上画出来


Note:
(1)在jupyter使用中, 如果要可视化模型预测结果, 一定要设置 %matplotlib inline  魔法命令, 并且所在行不能有注释, 否则无法使用, 因为 Ultralytics库(yolov8)在运行 plot() 或 show()时, 为了在服务器等无头(headless)环境下稳定运行, YOLO会强制将后端设置为了 Agg.
Agg后端是 Matplotlib 的一个基础后端, 主要用于生成静态图片文件, 这个后端只负责"画图"和"存图", 不负责"显示", 无法弹出独立的显示窗口, 故直接调用 plot.imshow() 会报错
而 %matplotlib inline 魔法命令的作用，就是将图形直接“嵌入”到 Notebook 的输出中，从而绕过了对 GUI 后端的需求
(2)jupyter的使用过程中, 要重新加载模型, 否则预测过程中的参数将不会更新, 除非手动再次给出.
如果说并不想重新加载模型, 可以将 比如说之前设置的 save_txt 参数拿回来, 然后强制的, 再把它设成 False, 这样的话, 它就不会被刚才我们所设置的参数所影响了
这个理解过程见"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\predict.ipynb"


3.数据集构建
(1)数据准备
其实, 本身这种检测, 都是对图片进行检测的("图片类型数据"), 如果说我们现在手头的数据就是图片类型的数据, 那这里我们就不需要再进行额外的处理了, 我们就可以直接进行标注
而如果先现在没有图片这种数据, 而是有视频类型数据("视频类型数据"), 那就要对它进行额外处理, 把它进行抽帧处理, 然后把它导成一张一张的图片

这里图片类型数据就不再演示了, 视频类型数据这里再来演示下抽帧部分代码该如何写, 将视频进行抽帧处理, 导出为图片
详见"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\extract.ipynb"  (extract提取)
右上角选择内核, 选择先前创建的yolov8环境
执行完前几个代码块后, 同样会遇到图片颜色空间通道需要转换的问题
---
# 接下来通过 matplotlib 的 pyplot 方法()来让它可视化一下
# plt.imshow(frame)  # 先前声明了 plt 是 matplotlib.pyplot 方法的别名
# 但此时也有和上一节讲的一样的问题 (见"D:\Tech\Code\Program\yolov8\ultralytics-main\program\jupyter\predict.ipynb")
# 即这里输出的这一帧 frame 为 BGR, 而不是 plt 所期望的 RGB 的三通道顺序, 故所得到的图颜色不正常
# 上一节是通过[:,:,::-1]这种方式来翻转通道的, 这里我们可以通过 OpenCV 的 cvtcolor() 方式, 来实现由 BGR 到 RGB 的转换, 其中 cv2.cvtcolor(图片, 模式), 图片选中之前的 frame 帧, 模式选为 cv2.COLOR_BGR2RGB 将图像的颜色空间从 BGR 转换为 RGB
plt.imshow(cv2.cvtColor(frame, cv2.COLOR_BGR2RGB))
---