#include <bits/stdc++.h>
using namespace std;

int main() {
    string word;
    vector<string> vec;
    while(cin >> word) {      //自行测试输入时要先按enter随后手动输入ctrl+z作为截止符才能停止循环
        vec.push_back(word); 
    }
    
    return 0;
}