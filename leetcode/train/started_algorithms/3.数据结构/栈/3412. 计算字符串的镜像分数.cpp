class Solution {
public:
    long long calculateScore(string s) {
        vector<int> a(26);
        for (int i = 0; i < s.size(); i++) {
            
        }
    }
};


// int c = 'a' + 'b'
// c - a[i] 为镜像
// c - a[i] - 'a' = 'b' - a[i] 为镜像下标